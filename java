class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
        int x =100;
        String s = Integer.toString(x);
        String s2 = "Hello from salem";
       
        System.out.println("ss"+s);
        s.startsWith("abc");
        s.endsWith("abc");
        s.compareTo(s2);
        s.indexOf("m");
        s.lastIndexOf("m");
        s.indexOf("m",startindexforthesearch);
        s.lastIndexOf("m",startindexforthesearch);
        String s3 = s2.substring(startindex);
        String s3 = s2.substring(startindex,endindex);
        s3.concat(s);
        s3.replace(ch,replacingchar);
        s3.trim();
        s.toLowerCase();
        s.toUpperCase();
        s.toString();
       
       
       
       
        StringBuffer sb = new StringBuffer();
        sb.append(str);
        sb.insert(index,str);
        sb.delete(start,end);
        sb.replace(start,end,str);
        sb.reverse();
        sb.capacity();
        sb.getChars(int srcstart,int srcstart,char arr[],int deststrt)
       
        sb.regionMatches(startindex,s1,startindex,noofcharacters)
           StringTokenizer st = new StringTokenizer("my name is khan"," ");  
        while (st.hasMoreTokens()) {  
         System.out.println(st.nextToken());  
     }  
         password.toCharArray()
         Character.isUpperCase(c)
         Character.isDigit(c)
         String compact = s.replaceAll(".?\\*", "");
         String s1 = "parliament";
         char a1[] = s1.toCharArray();
         str.deleteCharAt(0);
         Set seen = new HashSet();
         Set repeated = new HashSet();
         String ten = s.substring(i, i + 10);
         Arrays.deepToString(repeated.toArray())
         arr[i][j]=input.next().charAt(0);
